name: Easeatten Android CI

on:
  push:
    branches: [ "main" ]
  pull_request:
  workflow_dispatch:

jobs:
  ktfmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      # JetBrains JDK is required by Gradle project settings.
      - name: Setup JDK
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: jetbrains

      # Configure Gradle for optimal use in GitHub Actions, including caching
      # of downloaded dependencies.
      # See: https://github.com/gradle/actions/blob/main/setup-gradle/README.md
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      # Gradle tasks from `com.ncorti.ktfmt.gradle`. Configuration is managed
      # within the project settings.
      - run: ./gradlew ktfmtCheck ktfmtCheckScripts

  detekt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      # JetBrains JDK is required by Gradle project settings.
      - name: Setup JDK
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: jetbrains

      # Configure Gradle for optimal use in GitHub Actions, including caching
      # of downloaded dependencies.
      # See: https://github.com/gradle/actions/blob/main/setup-gradle/README.md
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      # Gradle tasks from `dev.detekt`. Configuration is managed within the
      # project settings.
      - run: ./gradlew detekt

  build:
    runs-on: ubuntu-latest
    needs: [ "ktfmt", "detekt" ]
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive

      - uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/git/db/
            ~/.cargo/registry/cache/
            ~/.cargo/registry/index/
            app/src/main/rust/sxcapi/target/
          key: ci-${{ runner.os }}-cargo-${{ hashFiles('app/src/main/rust/sxcapi/Cargo.lock') }}

      - name: Setup JDK
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: jetbrains

      # Configure Gradle for optimal use in GitHub Actions, including caching
      # of downloaded dependencies.
      # See: https://github.com/gradle/actions/blob/main/setup-gradle/README.md
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Setup Rust toolchain
        run: rustup target add armv7-linux-androideabi aarch64-linux-android x86_64-linux-android

      - name: Install Rust crate `cargo-ndk`
        uses: taiki-e/cache-cargo-install-action@v3
        with:
          tool: cargo-ndk

      - name: Install Rust crate `uniffi-bindgen`
        uses: taiki-e/cache-cargo-install-action@v3
        with:
          tool: uniffi
          features: cli

      - name: Build with Gradle
        run: ./gradlew build

      - name: Upload Artifact (debug APK)
        uses: actions/upload-artifact@v7
        with:
          name: apk-debug
          path: app/build/outputs/apk/debug
